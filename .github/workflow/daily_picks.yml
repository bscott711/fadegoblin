name: FadeGoblin Daily Picks

# --- CHANGED: Updated schedule for 6 AM and 11 AM Mountain Time ---
on:
  schedule:
    # 13:00 UTC = 6:00 AM MST (Morning degenerate picks)
    - cron: '0 13 * * *'
    # 18:00 UTC = 11:00 AM MST (Mid-day degenerate picks)
    - cron: '0 18 * * *'
  workflow_dispatch:
# ------------------------------------------------------------------

jobs:
  run-bot: